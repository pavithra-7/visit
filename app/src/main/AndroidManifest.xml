<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.visit">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.HomeActivity"/>
        <activity android:name=".activity.CreateUserActivity" />
        <activity android:name=".activity.UserListActivity" />
        <activity android:name=".activity.DeaprtmentListActivity">

            <!--
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            -->

        </activity>

        <activity android:name=".activity.DepartmentLoginActivity"/>
        <activity android:name=".activity.CreateDepartmentActivity" />
        <activity android:name=".activity.DepartmentHomeActivity" />
        <activity android:name=".activity.AdminHomeActivity" />
        <activity android:name=".activity.AdminLoginActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>